#!/bin/bash

if [ -d build ]; then
  rm -rf build
  echo "> Removed ./build"
fi
if [ -d "CMakeFiles" ]; then
  rm -rf CMakeFiles
  rm cmake_install.cmake CMakeCache.txt Makefile
  echo "> Removed autogenerated files"
fi
